{
    "name": "Phoenix + Postgres",
    "dockerComposeFile": "docker-compose.yml",
    "service": "web",
    "customizations": {
        "vscode": {
            "extensions": [
                "jakebecker.elixir-ls"
            ]
        }
    },
    "workspaceFolder": "../pento",
}
